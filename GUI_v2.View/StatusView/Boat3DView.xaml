<UserControl x:Class="GUI_v2.View.StatusView.Boat3DView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI_v2.View.StatusView" 
             xmlns:helix="http://helix-toolkit.org/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="{Binding ABC}" Width="100" Height="100" Background="Red"/>
        <helix:HelixViewport3D x:Name="viewPort3d" ZoomExtentsWhenLoaded="False" Grid.RowSpan="2">
                <!-- Remember to add light to the scene -->
            <ModelVisual3D Content="{Binding BoatModel}"/>
            <helix:DefaultLights/>
            </helix:HelixViewport3D>
        </Grid>
</UserControl>
